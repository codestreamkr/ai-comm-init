interface:
  display_name: "로드형"
  short_description: "프로젝트 컨벤션을 맞춰 Spring 작업을 정리"
  default_prompt: "Use $spring to inspect the project conventions first and handle this Spring task in the repository's existing style."
policy:
  allow_implicit_invocation: true
