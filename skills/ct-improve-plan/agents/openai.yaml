interface:
  display_name: "ct-improve-plan"
  short_description: "개선 과제를 분석해 우선순위와 계획 문서를 만드는 스킬"
  default_prompt: "Use $ct-improve-plan to analyze improvement candidates, prioritize them, and write a {project}_improve_plan.md document."
policy:
  allow_implicit_invocation: true
