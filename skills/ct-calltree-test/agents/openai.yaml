interface:
  display_name: "ct-calltree-test"
  short_description: "CallTree 문서 기반 Java 단위 테스트 생성"
  default_prompt: "Use $ct-calltree-test to read a CallTree document, use its [TC:✅] nodes and compact node metadata as hints, determine test strategy locally, and generate Java unit tests plus related MainTest artifacts."
policy:
  allow_implicit_invocation: true
