interface:
  display_name: "ct-calltree-test"
  short_description: "CallTree 문서를 바탕으로 Java 테스트를 생성"
  default_prompt: "Use $ct-calltree-test to read this CallTree document and generate the related Java unit tests and MainTest artifacts."
policy:
  allow_implicit_invocation: true
