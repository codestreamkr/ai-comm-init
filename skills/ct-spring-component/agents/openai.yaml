interface:
  display_name: "ct-spring-component"
  short_description: "명시한 Spring 컴포넌트를 프로젝트 기준에 맞게 추가"
  default_prompt: "Use $ct-spring-component <component-name> to add a supported Spring component with its required application points and validation."
policy:
  allow_implicit_invocation: false
