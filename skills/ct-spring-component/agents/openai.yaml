interface:
  display_name: "ct-spring-component"
  short_description: "명시한 Spring 컴포넌트 묶음을 추가"
  default_prompt: "Use $ct-spring-component service-log only when explicitly requested to add the predefined service-log Spring component."
policy:
  allow_implicit_invocation: false
