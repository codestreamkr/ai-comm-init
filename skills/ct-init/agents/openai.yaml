interface:
  display_name: "ct-init"
  short_description: "언어별 core 3문서와 AGENTS 라우팅 정리"
  default_prompt: "Use $ct-init to detect the project language, regenerate the core project documents, and reduce AGENTS.md to routing-only content."
policy:
  allow_implicit_invocation: true
