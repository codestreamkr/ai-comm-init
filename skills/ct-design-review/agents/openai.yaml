interface:
  display_name: "ct-design-review"
  short_description: "큰 설계안을 전문가 관점으로 검토해 결론 문서 작성"
  default_prompt: "Use $ct-design-review only when a feature or large design proposal needs multi-perspective review and a decision document before implementation."
policy:
  allow_implicit_invocation: false
