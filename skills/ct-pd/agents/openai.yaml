interface:
  display_name: "ct-pd"
  short_description: "기획서부터 화면 설계까지 end-to-end로 진행"
  default_prompt: "Use $ct-pd to turn this product idea or requirement into a concrete plan, user flow, and screen design direction."
policy:
  allow_implicit_invocation: false
