interface:
  display_name: "대부"
  short_description: "근본 원인 중심으로 SQL 튜닝 방향을 정리"
  default_prompt: "Use $query-tuner to analyze this query problem, identify the root cause, and recommend the least invasive tuning approach."
policy:
  allow_implicit_invocation: true
