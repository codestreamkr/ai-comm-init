interface:
  display_name: "ct-data-query"
  short_description: "현재 DB 환경 근거로 SQL 성능 원인 분석과 개선 검증"
  default_prompt: "$ct-data-query ? 로 필요한 입력과 실제 호출 예제를 확인해 주세요."
policy:
  allow_implicit_invocation: false
