interface:
  display_name: "ct-implement"
  short_description: "전파 범위가 큰 구현을 영향 범위까지 함께 처리"
  default_prompt: "Use $ct-implement only when this implementation affects APIs, data contracts, processing flows, multiple files, or requires related impact updates."
policy:
  allow_implicit_invocation: true
