interface:
  display_name: "ct-calltree"
  short_description: "Java 호출 흐름 분석과 CallTree 문서 생성"
  default_prompt: "Use $ct-calltree to analyze a Java controller or service and generate a structured CallTree with [TC:✅] nodes."
policy:
  allow_implicit_invocation: true
