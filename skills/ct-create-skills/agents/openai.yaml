interface:
  display_name: "ct-create-skills"
  short_description: "최소 입력을 받아 새 스킬 초안을 생성"
  default_prompt: "Use $ct-create-skills to ask for the minimum skill inputs first, then generate a draft skill from the provided skill name and purpose. Use only $skill-name and skill-name as triggers, and keep display_name identical to the skill name."
policy:
  allow_implicit_invocation: true
