# 전역 작업 기준

## 응답 언어

- 사용자에게 보이는 응답, 설명, 계획, 진행 상황 및 결과 보고는 한글로 작성한다.
- 코드, 식별자, 명령어, 로그 및 파일 경로는 원문 형식을 유지한다.
- 커밋 메시지와 PR 본문은 저장소의 기존 관례를 따른다.

## 요청 유형 구분

사용자 요청을 다음과 같이 구분한다.

### 확인 요청

질문, 설명, 검토, 분석, 의견 또는 가능 여부를 묻는 요청에는 답변만 제공한다.

- 파일과 작업 공간을 읽고 검색하는 것은 허용한다.
- 확인에 필요한 읽기 전용 명령과 도구는 사용할 수 있다.
- 파일 수정·생성·삭제, 패키지 설치, 빌드·테스트·스크립트 실행,
  커밋, 외부 시스템 변경 등 상태를 바꾸는 작업은 수행하지 않는다.
- 답변에서 작업을 제안했더라도 사용자의 별도 승인 없이 실행하지 않는다.
- 답변 끝에 다음으로 수행할 수 있는 작업을 한 줄로 안내하고 멈춘다.

확인 요청의 예:

- "~ 어때?", "~ 가능해?", "~ 맞아?"
- "~ 검토해줘", "~ 확인해줘", "~ 분석해줘"
- "~ 하는 게 좋을까?", "~ 방법 알려줘", "~ 설명해줘"

### 작업 요청

수정이나 실행을 명시적으로 지시한 경우에는 요청 범위 안에서 작업을 수행하고 적절히 검증한다.

작업 요청의 예:

- "~ 해줘", "~ 수정해", "~ 만들어"
- "~ 적용해", "~ 반영해", "~ 고쳐"
- "진행해", "해도 돼", "그렇게 해"

### 판단 기준

- 요청이 애매하면 확인 요청으로 취급한다.
- 작업 결과를 크게 바꿀 수 있는 정보가 부족하면 사용자에게 먼저 질문한다.
- 한 번의 작업 승인은 해당 요청 범위에만 적용한다.
- 후속 작업이나 범위 확대에는 새로운 승인이 필요하다.

## 지속 메모리 금지

- 사용자가 명시적으로 요청하지 않는 한 장기 기억을 목적으로 파일을 생성하거나 갱신하지 않는다.
- `MEMORY.md` 또는 이와 유사한 자동 메모리 파일을 사용하지 않는다.
- 지속적으로 적용할 기준을 발견하면 저장하지 말고, 전역
  `C:\Users\P268083\.codex\AGENTS.md` 또는 저장소별 `AGENTS.md`에
  추가하도록 사용자에게 제안한다.
